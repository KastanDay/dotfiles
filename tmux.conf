# TODO: make auto on new machines: https://github.com/tmux-plugins/tpm/blob/master/docs/automatic_tpm_installation.md
# TODO: Install plugins manually! git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# then do ctrl + a + shift + I (to install plugins after adding to conf.)

# reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.tmux/.tmux.conf 

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# split panes using | and -
bind | split-window -h
# bind \ split-window -h
bind - split-window -v
unbind '"'
unbind %

# Allows for faster key repetition
set -s escape-time 0

# Rather than constraining window size to the maximum size of any client
# connected to the *session*, constrain window size to the maximum size of any
# client connected to *that window*.
setw -g aggressive-resize on

# switch panes using Alt/Option-arrow without prefix
# Broken for left and right :(  but ctrl+a + left/right works. And clicking.
# bind -n M-Left select-pane -L
# bind -n M-Right select-pane -R
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -n M-Up select-pane -U     # option + up
bind -n M-Down select-pane -D   # option + down (left and right to word selection)

# Enable mouse mode (tmux 2.1 and above)
set -g mouse on

# don't rename windows automatically
set-option -g allow-rename off

######################
### DESIGN CHANGES ###
######################

# loud or quiet?
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

#  modes
setw -g clock-mode-colour colour5
setw -g mode-style 'fg=colour1 bg=colour18 bold'

# panes
set -g pane-border-style 'fg=colour19 bg=colour0'
set -g pane-active-border-style 'bg=colour0 fg=colour9'

# statusbar
set -g status-position bottom
set -g status-justify left
# set -g status-style 'bg=colour18 fg=colour137 dim'
set -g status-style 'bg=#333333 fg=#5eacd3'
set -g status-left ''
set -g status-right '#[fg=colour233,bg=colour19] %d/%m #[fg=colour233,bg=colour8] %H:%M:%S '
set -g status-right-length 90
set -g status-left-length 50

setw -g window-status-current-style 'fg=colour1 bg=colour19 bold'
setw -g window-status-current-format ' #I#[fg=colour249]:#[fg=colour255]#W#[fg=colour249]#F '

# setw -g window-status-style 'fg=colour9 bg=colour18'
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

setw -g window-status-bell-style 'fg=colour255 bg=colour1 bold'

# messages
set -g message-style 'fg=colour232 bg=colour16 bold'

#### NEW COLORS
# Dracula Colours
# background_color '#282a36'
# current_line_color '#44475a'
# foreground_color '#f8f8f2'
# comment_color '#6272a4'
# cyan '#8be9fd'
# green '#50fa7b'
# orange '#ffb86c'
# pink '#ff79c6'
# purple '#bd93f9'
# red '#ff5555'
# yellow '#f1fa8c'

set -g default-terminal "screen-256color"

# pane border
set -g pane-border-style fg='#6272a4'
set -g pane-active-border-style fg='#ff79c6'

# message text
set -g message-style bg='#44475a',fg='#8be9fd'

# clock
set-window-option -g clock-mode-colour '#50fa7b'

# status line
set -g status-style bg='#44475a',fg='#bd93f9'
set -g status-interval 1

# status left
set -g status-left '#[bg=#bd93f9]#[fg=#44475a]#{?client_prefix,#[bg=#ff79c6],} 😂 #[fg=#bd93f9]#[bg=#44475a]'

# are we zoomed into a pane?
set -ga status-left '#[bg=#44475a]#[fg=#ff79c6] #{?window_zoomed_flag, ↕  ,}'

# window status
set-window-option -g window-status-style fg='#bd93f9',bg=default
set-window-option -g window-status-current-style fg='#ff79c6',bg='#282a36'

set -g window-status-current-format "#[fg=#44475a]#[bg=#bd93f9]#[fg=#f8f8f2]#[bg=#bd93f9] #I #W #[fg=#bd93f9]#[bg=#44475a]"
set -g window-status-format "#[fg=#f8f8f2]#[bg=#44475a]#I #W #[fg=#44475a] "

# status right
set -g status-right '#[fg=#f1fa8c,bg=#44475a]#[fg=#44475a,bg=#f1fa8c] #(hostname)'
set -ga status-right '#[fg=#8be9fd,bg=#f1fa8c]#[fg=#44475a,bg=#8be9fd] CPU #{cpu_icon} #{cpu_percentage} RAM #{ram_icon} #{ram_percentage}'
# set -ga status-right '#[fg=#ff79c6,bg=#8be9fd]#[fg=#44475a,bg=#ff79c6] Uptime: #(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") '  # no more uptime!
set -ga status-right '#[fg=#bd93f9,bg=#8be9fd]#[fg=#f8f8f2,bg=#bd93f9] %a %H:%M:%S #[fg=#6272a4]%h-%d '

#------------------------------------
# Plugins  --  https://github.com/tmux-plugins/tpm
#------------------------------------

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-cpu'   # CPU plugin: https://github.com/tmux-plugins/tmux-cpu
# set -g @plugin 'tmux-plugins/tmux-sensible'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# removed -b flag
run '~/.tmux/plugins/tpm/tpm'

